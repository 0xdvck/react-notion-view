


//should extact to constant variable
const notionSupportType = ['checkbox', 'title', 'number', 'rich_text']

const typeMappingForFilterAndSort = {
    'title': 'rich_text',
}

module.exports = {
    notionSupportType,
    typeMappingForFilterAndSort
}